#include <iostream>
#include <string.h>

using namespace std;
int main() {
    char arr[101];
    int max = 0;

    cin >> arr;
    max = strlen(arr);

    cout << max;
    
    return 0;
}