#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
#include <set>
#include <stack>
#include <list>

using namespace std;

int main() {
    int k, q, r, v, n, p, k1 = 1, q1 = 1, r1 = 2, v1 = 2, n1 = 2, p1 = 8;

    cin >> k >> q >> r >> v >> n >> p;

    cout << k1 - k << " " << q1 - q << " " << r1 - r << " " << v1 - v << " " << n1 - n << " " << p1 - p;

    return 0;
}